{
  "name": "corpseclient-website",
  "version": "1.0.0",
  "private": true,
  "description": "CorpseClient site + API + admin panel (GitHub release)",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "dev:admin": "npm run dev --prefix frontend-admin",
    "start": "npm run start --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend-admin"
  }
}
